import React from 'react';
import useUser from '../../hooks/useUser';

const AddGuild = () => {
    return <div>AddGuild</div>;
};

const AddGuildWrapper = () => {
    // const [user] = useUser();

    // if (!user) {
    //     window.open('/');
    // }
    return <AddGuild />;
};

export default AddGuild;
