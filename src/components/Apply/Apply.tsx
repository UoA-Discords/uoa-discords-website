import React from 'react';

/** The apply page handles making new applications for guilds. */
const Apply = () => {
    return <div>Apply</div>;
};

const ApplyWrapper = () => {};

export default Apply;
